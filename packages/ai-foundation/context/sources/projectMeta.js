// Placeholder context source — not wired into any Phase 1 contextRequirements.
// There is no "projects" collection in scope yet (Phase 2+). Present here only
// to match the package's folder scaffold for future capabilities.
async function getProjectMeta() {
  return null;
}

module.exports = { getProjectMeta };
